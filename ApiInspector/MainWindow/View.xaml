<Window x:Class="ApiInspector.MainWindow.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApiInspector"
        xmlns:components = "clr-namespace:ApiInspector.Components"
        xmlns:application = "clr-namespace:ApiInspector.Application"
        xmlns:invocationInfoEditor = "clr-namespace:ApiInspector.InvocationInfoEditor"
        mc:Ignorable="d"
        x:Name="CodeBehind"
        DataContext="{Binding ElementName=CodeBehind}"
        Title="View" Height="700" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
           <StackPanel>
               <Label FontWeight="Bold">History</Label>
               <TextBox></TextBox>
           </StackPanel>
            <ListBox Grid.Row="1" x:Name="historyListBox" SelectionChanged="HistoryListBox_OnSelected">

            </ListBox>
        </Grid>

        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" />

        <Grid Grid.Column="2" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel  >
                <Button HorizontalAlignment="Right" Click="OnExecuteClicked"> Execute</Button>
               
                <invocationInfoEditor:View x:Name="currentInvocationInfo"  Margin="0,10,0,0" />

                <StackPanel Margin="0,10,0,0">

            
                    <Label FontWeight="Bold"> Response </Label>
                 
                </StackPanel>

               

            </StackPanel>
            <RichTextBox Grid.Row="1" Height="Auto" x:Name="invokingResponseView" ></RichTextBox>
        </Grid>
        

    </Grid>
</Window>
